CREATE TABLE RESIDENCIAS 
(
  IDOPERARIO NUMBER NOT NULL 
, NIT NUMBER NOT NULL 
, NOMBRE VARCHAR2(30) NOT NULL 
, DIRECCION VARCHAR2(40) NOT NULL 
, TELEFONO NUMBER NOT NULL 
, CONSTRAINT RESIDENCIAS_PK PRIMARY KEY 
  (
    IDOPERARIO 
  )
  ENABLE 
);

ALTER TABLE RESIDENCIAS
ADD CONSTRAINT RESIDENCIAS_UK1 UNIQUE 
(
  NIT 
)
ENABLE;

ALTER TABLE RESIDENCIAS
ADD CONSTRAINT RESIDENCIAS_FK1 FOREIGN KEY
(
  IDOPERARIO 
)
REFERENCES OPERARIOS
(
  ID 
)
ENABLE;
